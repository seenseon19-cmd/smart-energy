import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.smartenergy',
  appName: 'SmartEnergy',
  webDir: 'dist/client'
};

export default config;
